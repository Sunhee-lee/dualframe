package com.dualframe

import android.app.Application

/**
 * Application subclass. Currently minimal — exists as a hook for
 * future initialization (analytics, crash reporting, etc.).
 */
class DualFrameApp : Application()
