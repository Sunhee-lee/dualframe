<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.camera" android:required="true" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.android.vending.BILLING" />

    <!--
      Package visibility for BottomActionBar.buildGalleryIntent, which finds the
      gallery by two lookups and both are queries: getLaunchIntentForPackage on
      the Samsung packages and resolveActivity on the APP_GALLERY intent.
      Without these they are filtered and return null, leaving the button on the
      video collection URI — a plain list rather than the gallery app.

      Observed on Samsung the gallery resolves even without this block, OEM
      system apps commonly being exempt; these entries cover the devices where
      they are not.
    -->
    <queries>
        <package android:name="com.sec.android.gallery3d" />
        <package android:name="com.samsung.android.gallery" />
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.APP_GALLERY" />
        </intent>
    </queries>

    <application
        android:name="com.dualframe.DualFrameApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.DualFrame">

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${admobAppId}" />

        <activity
            android:name="com.dualframe.ui.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
